# Dudley landing pages

## Run locally
1. npm install
2. npm run dev

## Notes
- This package excludes node_modules and dist to keep the zip small.
- It includes only the assets currently referenced by the source code.
- The white-screen crash in PeppermintCombo.jsx was fixed.
- Click the main product images to trigger the 360 spin interaction.
